# Image Pipeline

Run `npm run optimize:images` from the website root. The default mode creates missing WebP variants for large raster files and leaves source files untouched. Review generated files before replacing page references.

High-resolution originals belong in `media/source/` or an external archive. AI-generated visuals used on public pages must be described as concept visualizations when they do not document a completed project.

