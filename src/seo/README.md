# SEO Architecture

This folder will hold route metadata and builders for:

- canonical URLs
- Open Graph and Twitter metadata
- language declarations and `hreflang`
- Organization, LocalBusiness, Service, Article, Breadcrumb, and FAQ schema
- sitemap and route validation helpers

All production canonicals must use `https://www.ateliernusa.id`.
